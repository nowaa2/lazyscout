/**
 * Mirrors the placeholder the server writes for password fields. Kept here so
 * the web bundle does not need to import the Playwright-backed explorer.
 */
export const SECRET_PLACEHOLDER = '{{TEST_PASSWORD}}'
