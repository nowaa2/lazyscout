# Fixtures

Everything under `fixtures/` is synthetic test data for local development. It does not contain customer, employee, production or reusable account data.

The demo site accepts arbitrary values and must not be connected to a production service.
